# Injectable
Il s'agit d'une annotation que l'on positionne sur un composant : service ou autre pour indiquer que la classe devient une entité injectable sous la forme d'un singleton ou non.

# Component / constructor
Dans le constructeur, on peut mettre en paramètre les injectables.
L'injectable est alors créé et devient disponible dans le composant ; une proprieté du nom de l'injectable est alors créé automiquement.

# Import
@angular/core
